int main()
{
    decl 
        int temp;
        string fileName;
    enddecl

    begin
        temp = exposcall("Write", -2, "File Name:");
        temp = exposcall("Read", -1, fileName);
        temp = exposcall("Delete",fileName);
        return 0;
    end
}